spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: 1234
        url: jdbc:postgresql://localhost:5432/peak_space
        username: postgres
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true

