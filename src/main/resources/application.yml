spring:
  config:
    import:
      - classpath:/configs/database-config.yml
      - classpath:/configs/email-config.yml
      - classpath:/configs/flyway-config.yml
      - classpath:/configs/amazon-services-config.yml

# Security
app:
  jwt:
    secret: peak-space-java12

server:
  error:
    include-stacktrace: always
