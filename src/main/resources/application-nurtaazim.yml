spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: aliaskartemirbekov@gmail.com
    password: izcp edth hxpv nkgl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 1234
    url: jdbc:postgresql://localhost:5432/peakspace
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false

app:
  jwt:
    secret: Java12
    #When you will click 'create' you shout click to 'always'! from Myrzaiym
    defer-datasource-initialization: true
  sql:
    init:
      data-locations: classpath:dataInit/dataInit.sql
      mode: never

      #  #DATA-INIT
      #  spring.jpa.defer-datasource-initialization=true
      #  spring.sql.init.data-locations=classpath:datainit/dataInit.sql
      #  spring.sql.init.mode=never